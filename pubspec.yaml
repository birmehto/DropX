name: dropx
description: A highly customizable, theme-aware dropdown widget for Flutter with full keyboard navigation, search functionality, and auto-positioning.
version: 0.0.2
homepage: https://github.com/birmehto/dropx
repository: https://github.com/birmehto/dropx
issue_tracker: https://github.com/birmehto/dropx/issues

topics:
  - dropdown
  - widget
  - ui
  - keyboard-navigation
  - search

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
